[build]
buildCommand = "npm install"

[deploy]
startCommand = "mkdir -p dist/views dist/public && npx tsc -p tsconfig.json && cp -R src/views/. dist/views/ && cp -R src/public/. dist/public/ && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
