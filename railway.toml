[build]
buildCommand = "npm install"

[deploy]
startCommand = "mkdir -p dist/views dist/public && cp -R src/views/. dist/views/ && cp -R src/public/. dist/public/ && npx tsx src/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
